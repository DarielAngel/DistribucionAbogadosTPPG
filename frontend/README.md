# Provincia Abogados - Frontend

Scaffold Vite + Vue 3 + Tailwind minimal.

Setup

```bash
cd frontend
npm install
npm run dev
```

Configure API base URL by setting `VITE_API_URL` in your environment or `.env` file (e.g. `VITE_API_URL=http://localhost:4000/api`).
