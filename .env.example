JWT_SECRET=change-me-in-production
PORT=80

# Postgres (used by docker-compose to provision the `db` service and to build
# the backend's DATABASE_URL). Change these before deploying.
POSTGRES_USER=abogados
POSTGRES_PASSWORD=change-me-in-production
POSTGRES_DB=abogados_db
